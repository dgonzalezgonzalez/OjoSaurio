# Uso diario

1. Abre app.
2. Pulsa `Start`.
3. Trabaja normal.
4. Al `beep 1` (20 min), mira lejos.
5. Al `beep 2` (20 s), vuelve al trabajo.

Si haces descanso largo, usa `Pause`. Al volver, usa `Resume`.
